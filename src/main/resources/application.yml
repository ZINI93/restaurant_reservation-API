
spring:
  application:
    name: restaurant_reservation
  datasource:
    url: jdbc:mysql://localhost:3307/restaurant_reservation?zeroDateTimeBehavior=convertToNull&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: 
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
  batch:
    jdbc:
      initialize-schema: always

